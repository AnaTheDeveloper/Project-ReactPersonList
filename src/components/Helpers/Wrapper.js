const Wrapper = (props) => {
    return props.children;

    //the children prop holds all the content
    // you're passing between the opening and closing tag
    // off your custom component.

};

export default Wrapper;